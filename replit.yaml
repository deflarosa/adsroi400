
modules:
  - name: python
    language: python3
    version: 3.11

run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000

install:
  command: pip install -r requirements.txt

env:
  PORT: 8000
